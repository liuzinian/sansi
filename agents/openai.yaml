interface:
  display_name: "三思"
  short_description: "先问清需求，再规格设计"
  default_prompt: "Use $sansi to scope requirements first, save the understood need, clarify grouped ambiguities, then produce specifications and design only; stop before implementation unless explicitly authorized."
